{
  "name": "porygon-industrial-os",
  "version": "1.0.0",
  "private": true,
  "description": "Full-scale industrial intelligence platform - Digital Twin + Simulator",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:twin": "cd digital-twin && npm run dev",
    "dev": "npm run dev:frontend & cd digital-twin && npm run dev",
    "build": "npm run build --workspace=frontend",
    "install:all": "npm install && cd digital-twin && npm install"
  }
}
